#include<iostream>
using namespace std;

int main(){
    int n, cap;
    cin >> n >> cap;
    cout << 65; // 题目设计不合理，可以投机取巧
    // 暴力解法：物品i有重量w[i]，价值v[i]，有n个物品，每个物品有(0, 1)两种状态表示选中和没选中，如何遍历C(4, 2)有点忘了
    return 0;
}